# starlette fixado acima de 1.1.0 por causa do CVE-2026-48818 (SSRF e roubo de
# credencial NTLM por caminho UNC no StaticFiles). O portao do Trivy pegou isso
# no primeiro build — a versao que o fastapi puxava sozinho era a 0.49.3.
fastapi==0.141.1
starlette==1.6.0
uvicorn[standard]==0.41.0
jinja2==3.1.6
prometheus-client==0.24.1
# So a API, nao o SDK: quem instrumenta e o operador do OpenTelemetry no
# cluster, injetando a auto-instrumentacao no pod. A API entra aqui para o log
# conseguir ler o trace_id do span atual — e, fora do cluster, ela simplesmente
# nao encontra span nenhum e o log sai sem os campos de correlacao.
opentelemetry-api==1.39.0
