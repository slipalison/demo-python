# starlette fixado acima de 1.1.0 por causa do CVE-2026-48818 (SSRF e roubo de
# credencial NTLM por caminho UNC no StaticFiles). O portao do Trivy pegou isso
# no primeiro build — a versao que o fastapi puxava sozinho era a 0.49.3.
fastapi==0.141.1
starlette==1.6.0
uvicorn[standard]==0.41.0
jinja2==3.1.6
prometheus-client==0.24.1
# TRACES NO PROPRIO APP, e nao pela injecao do operador do OpenTelemetry.
#
# A auto-instrumentacao do operador funciona por webhook de admissao, e o
# webhook dele estava com "bad certificate" e failurePolicy: Ignore — ou seja,
# falhando CALADO: o pod nascia sem instrumentacao nenhuma e nada no cluster
# dizia por que. (O chart regenera o certificado a cada sync e o caBundle do
# webhook ficou congelado no antigo.)
#
# Instrumentar aqui troca uma dependencia de infraestrutura invisivel por dez
# linhas visiveis em observabilidade.py. Sem OTEL_EXPORTER_OTLP_ENDPOINT no
# ambiente, nada disso liga — rodar local continua simples.
opentelemetry-api==1.39.0
opentelemetry-sdk==1.39.0
opentelemetry-exporter-otlp-proto-http==1.39.0
opentelemetry-instrumentation-fastapi==0.60b0
